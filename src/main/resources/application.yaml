##===== server port =====##
server:
  port: 8088

##===== active profiles =====##
spring:
  profiles:
    active: prod

  application:
    name: abeille-gateway

  cloud:
    consul:
      discovery:
        register: false